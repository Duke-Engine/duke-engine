Prop
  Name = Pillar
  DisplayName = Pillar
  KindOf = [STRUCTURE]
  VisionRange = 0
  Geometry = Cylinder
    Radius = 3
    Height = 12
  End
  ; Three across, so two of them do not close a two-cell corridor — they are
  ; scattered in rooms rather than in corridors, but a room's edge is a doorway.


  Weight = 4
End
