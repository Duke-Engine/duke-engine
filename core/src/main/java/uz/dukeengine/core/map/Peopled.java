package uz.dukeengine.core.map;

/** A map drawn for so many players: what a lobby offers and what a start position is counted against. */
public interface Peopled extends MapTemplate {

    int players();
}
